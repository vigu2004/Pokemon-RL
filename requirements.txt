# Core
numpy
pandas

# Kaggle data/engine download + submission
kaggle

# RL stack
torch                 # policy/value net + custom PPO self-play

# Not used: the action space is a variable-length *set selection*, which doesn't
# fit gym/SB3's fixed Discrete/Box spaces — we implement PPO directly (src/selfplay.py).
# gymnasium
# stable-baselines3
# ray[rllib]          # scalable self-play, optional (later)
