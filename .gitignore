.venv/
__pycache__/
*.pyc
.DS_Store
.ipynb_checkpoints/

# Kaggle credentials — NEVER commit
kaggle.json
.kaggle/

# Large engine / data artifacts (keep CSV refs, ignore the heavy stuff)
data/*.pdf
engine/                 # native libs + sample agent; download via scripts/download_data.sh
episodes/               # ~750MB/day ladder replay datasets
*.tar.gz
*.zip

# Training artifacts
checkpoints/
models/
runs/
wandb/
*.pt
*.ckpt

# Submission staging (tarball already covered by *.tar.gz)
build/

# Local env / secrets
.env
.python-version
